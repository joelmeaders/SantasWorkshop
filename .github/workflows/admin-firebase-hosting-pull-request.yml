# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Admin Deploy to Firebase Hosting on PR
on: 
  pull_request:
    paths:
      - 'santashop-admin/**'

env: 
  FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}

jobs:
  build_and_preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci --prefix santashop-admin && npm run build --prefix santashop-admin
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_SANTAS_WORKSHOP_193B5 }}'
          target: admin
          projectId: santas-workshop-193b5
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
