name: App Test Deploy and Manual Prod Release
'on':
  push:
    branches:
    - master
    paths:
    - santashop-app/**
    - santashop-core/**
    - santashop-models/**
    - remote-config/**
    - scripts/remote-config*.cjs
    - scripts/public-parameters-schema.cjs
    - scripts/verify-public-parameters.cjs
    - .storybook/**
    - angular.json
    - eslint.config.js
    - vitest.config.mjs
    - vitest.setup.ts
    - test-helpers/**
    - package.json
    - pnpm-lock.yaml
    - pnpm-workspace.yaml
    - config.firebase.cjs
    - firebase.json
    - firebase.e2e.json
    - firestore.rules
    - storage.rules
    - santashop-e2e/fixtures/**
    - santashop-e2e/scripts/**
    - santashop-e2e/tests/public/**
    - santashop-e2e/playwright.config.ts
    - scripts/env-loader.cjs
    - tsconfig.storybook.json
    - vitest.storybook.config.ts
    - scripts/verify-hosting-styles.mjs
    - .github/workflows/app-test-and-prod-release.yml
    - .github/workflows/app-pr-validation.yml
    - .github/workflows/ui-target.yml
    - .github/workflows/release-gate.yml
    - scripts/release-*.mjs
  workflow_dispatch:
    inputs:
      release_ref:
        description: Full 40-character commit SHA on master
        required: true
        type: string
      deployment_target:
        description: Deployment target
        required: true
        default: prod
        type: choice
        options:
        - test
        - prod
      skip_tests:
        description: Reuse verified exact-SHA tests without rerunning suites
        required: true
        default: false
        type: boolean
      evidence_run_ids:
        description: Comma-separated successful validation and test-deployment run IDs
        type: string
        default: ''
      production_approval:
        description: Owner approval - repeat the full SHA to approve these evidence runs for production
        type: string
        default: ''

permissions:
  contents: read
  actions: read

jobs:
  prepare_release:
    uses: ./.github/workflows/release-gate.yml
    with:
      target: app
      mode: ${{ github.event_name == 'workflow_dispatch' && inputs.deployment_target || 'test' }}
      release_ref: ${{ github.event_name == 'workflow_dispatch' && inputs.release_ref || github.sha }}
      skip_tests: ${{ github.event_name == 'workflow_dispatch' && inputs.skip_tests || false }}
      evidence_run_ids: ${{ inputs.evidence_run_ids || '' }}
      production_approval: ${{ inputs.production_approval || '' }}

  release:
    needs: prepare_release
    uses: ./.github/workflows/ui-target.yml
    with:
      target: app
      mode: ${{ github.event_name == 'workflow_dispatch' && inputs.deployment_target || 'test' }}
      release_ref: ${{ needs.prepare_release.outputs.sha }}
      deploy: true
      skip_tests: ${{ github.event_name == 'workflow_dispatch' && inputs.skip_tests || false }}
    secrets:
      TEST_FIREBASE_API_KEY: ${{ secrets.TEST_FIREBASE_API_KEY }}
      PROD_FIREBASE_API_KEY: ${{ secrets.PROD_FIREBASE_API_KEY }}
      FIREBASE_SERVICE_ACCOUNT_SANTAS_WORKSHOP_TEST: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SANTAS_WORKSHOP_TEST }}
      FIREBASE_SERVICE_ACCOUNT_SANTAS_WORKSHOP_193B5: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_SANTAS_WORKSHOP_193B5 }}
