import { ChildInitialsPipe } from './child-initials.pipe';

describe('ChildInitialsPipe', () => {
  it('create an instance', () => {
    const pipe = new ChildInitialsPipe();
    expect(pipe).toBeTruthy();
  });
});
