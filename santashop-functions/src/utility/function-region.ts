/**
 * Matches the currently deployed Firebase Functions region for this project.
 * Keeping it explicit avoids accidental drift from Firebase default behavior.
 */
export const FUNCTION_REGION = 'us-central1';
